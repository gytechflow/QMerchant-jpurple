<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/booking_create_and_edit_graph"
    app:startDestination="@id/show_create_booking_fragment">
    <fragment
        android:id="@+id/show_create_booking_fragment"
        android:name="cm.clear.qmerchant.modules.bookings.createNewBooking.ui.CreateBookingFragment"
        android:label="Create"
        tools:layout="@layout/fragment_create_booking">
        <argument
            android:name="is_update"
            android:defaultValue="false"
            app:argType="boolean" />
        <argument
            android:name="booking_id"
            android:defaultValue="-1"
            app:argType="string" />
    </fragment>
</navigation>
